import numpy as np

# 'i' integers, 'f' floating point numbers, 'S' string, 'uint' unsigned integers if we want to represent numbers greater than 0
my_array = np.array([1, 2, 3], dtype='uint')
print(my_array.dtype)